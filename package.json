{
  "name": "super-sim-send-sms",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run --env"
  },
  "devDependencies": {
    "twilio": "^3.56.0",
    "twilio-run": "^2.0.0"
  },
  "engines": {
    "node": "8.10.0"
  }
}
